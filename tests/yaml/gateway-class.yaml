apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: example-gateway-class
  labels:
    tier: gateway
  annotations:
    gateway/controller: example-controller
spec:
  controllerName: example.com/gateway-controller