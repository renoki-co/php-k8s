apiVersion: v1
kind: PersistentVolume
metadata:
  name: app
spec:
  awsElasticBlockStore:
    fsType: ext4
    volumeID: vol-xxxxx
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  mountOptions:
    - debug
  storageClassName: sc1
